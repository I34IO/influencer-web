# Backend API Configuration
# This is the actual backend API that the Next.js API routes proxy to
NEXT_PUBLIC_API_BASE_URL=https://influencer-web-black.vercel.app/api

# Optional: API timeout in milliseconds (default: 10000)
# NEXT_PUBLIC_API_TIMEOUT=10000

# Optional: Enable API debug logging (default: false)
# NEXT_PUBLIC_API_DEBUG=true
